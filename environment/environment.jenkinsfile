pipeline {
    agent {label 'win10'}
    stages {
        stage('Build and Deploy') {
            steps {
                bat 'dir'
                bat '''
                docker-compose -f D:\\jenkins\\automation\\workspace\\first_pipeline\\environment\\docker-compose.yml build
                docker-compose -f D:\\jenkins\\automation\\workspace\\first_pipeline\\environment\\docker-compose.yml up -d
                '''
            }
}}}