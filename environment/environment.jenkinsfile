pipeline {
    agent {label 'win10'}
    stages {
        stage('Build and Deploy') {
            steps {
                bat 'dir'
                bat 'docker compose up -d D:\jenkins\automation\workspace\first_pipeline\environment\docker-compose.yml'
            }
}}}