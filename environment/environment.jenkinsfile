pipeline {
    agent {label 'win10'}
    stages {
        stage('Build and Deploy') {
            steps {
                bat 'dir'
                bat '''
                docker compose build -f D:\\jenkins\\automation\\workspace\\first_pipeline\\environment\\docker-compose.yml
                docker compose up -d -f D:\\jenkins\\automation\\workspace\\first_pipeline\\environment\\docker-compose.yml
                '''
            }
}}}