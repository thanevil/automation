pipeline {
    agent {label 'win10'}
    stages {
        stage('Build and Deploy') {
            steps {
                git "https://github.com/thanevil/automation.git"
                // Additional steps for AD setup and IP configuration
                bat 'dir'
            }
}}}